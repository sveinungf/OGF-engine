CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(glewlib)

FIND_PACKAGE(OpenGL REQUIRED)

INCLUDE_DIRECTORIES(
	include
	${OPENGL_INCLUDE_DIR}
)

ADD_LIBRARY(${GLEW_NAME} src/glew.c)

TARGET_LINK_LIBRARIES(${GLEW_NAME}
	${OPENGL_LIBRARIES}
)
